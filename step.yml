title: "apk explorer"
summary: Extract ids from apk ;)
description: |-
  Parse aapt dump badging .
website: https://github.com/...
source_code_url: https://github.com/...
support_url: https://github.com/.../issues
host_os_tags:
  - osx-10.10
  - linux
project_type_tags:
  - android
type_tags:
  - script
is_requires_admin_user: true
is_always_run: false
is_skippable: false
deps:
  brew:
  - name: git
  apt_get:
  - name: git
run_if: ""
inputs:
  - example_step_input: Default Value - you can leave this empty if you want to
    opts:
      title: "Example Step Input"
      summary: Summary. No more than 2-3 sentences.
      description: |
        Description of this input.

        Can be Markdown formatted text.
      is_expand: true
      is_required: true
      value_options: []
outputs:
  - APK_BUILD_VERSION:
    opts:
      title: "APK built versionCode"
      summary: extract the computed.
      description: |
        Description of this output.

        Can be Markdown formatted text.
